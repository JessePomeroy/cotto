{
  "name": "sotto-launch-video",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "prepare:assets": "node prepare-assets.mjs",
    "check": "npm run prepare:assets && hyperframes check .",
    "render": "npm run prepare:assets && hyperframes render . --output renders/sotto-launch.mp4 --fps 30 --quality high --workers 4",
    "preview": "python3 serve.py"
  },
  "devDependencies": {
    "gsap": "3.15.0",
    "hyperframes": "0.8.30"
  }
}
